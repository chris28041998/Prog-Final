
CREATE DATABASE progene;
use progene;
CREATE table users(
                   iduser int(5),
                   nom varchar(50),
                   usename varchar(30),
                   passw varchar(30),
                   primary key(iduser));

use progene;
insert into users()
